<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <title>Giỏ hàng của bạn</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      padding: 20px;
      background-color: #f4f4f4;
    }

    h1 {
      text-align: center;
      color: #333;
    }

    table {
      width: 100%;
      border-collapse: collapse;
      margin-top: 20px;
      background-color: #fff;
    }

    th, td {
      padding: 10px;
      text-align: left;
      border-bottom: 1px solid #ccc;
    }

    th {
      background-color: #007bff;
      color: white;
    }

    .total {
      font-weight: bold;
      text-align: right;
      margin-top: 20px;
    }

    .empty {
      text-align: center;
      color: #888;
      margin-top: 50px;
    }

    .voucher-form {
      margin-top: 20px;
      text-align: center;
    }

    .voucher-info {
      color: green;
      text-align: center;
      margin-top: 10px;
    }

    .discount {
      color: red;
    }
  </style>
</head>
<body>
  <%- include('../partials/header') %>

  <h1>Giỏ hàng của bạn</h1>

  <% if (cartItems.length === 0) { %>
    <p class="empty">Giỏ hàng trống</p>
  <% } else { %>
    <table>
      <thead>
        <tr>
          <th>Sản phẩm</th>
          <th>Hình ảnh</th>
          <th>Số lượng</th>
          <th>Giá</th>
          <th>Thành tiền</th>
          <th>Thao tác</th>
        </tr>
      </thead>
      <tbody>
        <% cartItems.forEach(item => { %>
          <tr>
            <td><%= item.product.name %></td>
            <td><img src="<%= item.product.urlImg %>" alt="" width="80"></td>
            <td><%= item.quantity %></td>
            <td><%= item.product.price %>₫</td>
            <td><%= item.product.price * item.quantity %>₫</td>
            <td><a href="#" onclick="removeItem('<%= item.product._id %>'); return false;">❌ Xoá</a></td>
          </tr>
        <% }) %>
      </tbody>
    </table>

    <a href="#" onclick="clearCart(); return false;">🗑️ Xoá toàn bộ giỏ hàng</a>

    <form action="/cart/voucher/apply" method="POST">
      <input type="text" name="code" placeholder="Nhập mã giảm giá">
      <button type="submit">Áp dụng</button>
    </form>

    <% if (voucher) { %>
      <div class="voucher-info">
        Đã áp dụng voucher: <strong><%= voucher.code %></strong> (-<%= voucher.discount %>%)
        <br>
        Giảm giá: <span class="discount"><%= discount %>₫</span>
      </div>
    <% } %>

    <p class="total">Tổng cộng: <%= total %>₫</p>
    <% if (voucher) { %>
      <p class="total">Tổng thanh toán sau giảm: <%= finalTotal %>₫</p>
    <% } %>

  <% } %>

  <div style="text-align: center; margin-top: 20px;">
    <a href="/products/view/all">Quay lại danh sách sản phẩm</a>
    <a href="/Cart/checkout" style="margin-left: 20px;">Thanh toán</a>
  </div>

  <%- include('../partials/footer') %>

<script>
  function removeItem(productId) {
    if (confirm('Xoá sản phẩm này?')) {
      fetch(`/cart/remove/${productId}`, {
        method: 'DELETE'
      })
      .then(res => res.json())
      .then(data => {
        alert(data.message || 'Đã xoá');
        location.reload();
      });
    }
  }

  function clearCart() {
    if (confirm('Xoá toàn bộ giỏ hàng?')) {
      fetch('/cart/clear', {
        method: 'DELETE'
      })
      .then(res => res.json())
      .then(data => {
        alert(data.message || 'Đã xoá toàn bộ');
        location.reload(); 
      })
      .catch(err => console.error('Lỗi khi xoá:', err));
    }
  }
</script>

</body>
</html>
