<!DOCTYPE html>
<html lang="vi">
<head>
  <meta charset="UTF-8">
  <title>Sửa Voucher</title>
</head>
<body>

<h1>Sửa Voucher</h1>

<form action="/voucher/<%= voucher._id %>" method="POST">
  <label>Mã Voucher:</label>
  <input type="text" name="code" value="<%= voucher.code %>" required>

  <label>Phần trăm giảm giá (%):</label>
  <input type="number" name="discount" value="<%= voucher.discount %>" min="1" max="100" required>

  <label>Ngày bắt đầu:</label>
  <input type="date" name="startDate" value="<%= voucher.startDate.toISOString().split('T')[0] %>" required>

  <label>Ngày kết thúc:</label>
  <input type="date" name="endDate" value="<%= voucher.endDate.toISOString().split('T')[0] %>" required>

  <button type="submit">Cập nhật</button>
</form>

<a href="/voucher/view/all">Quay lại</a>

</body>
</html>
