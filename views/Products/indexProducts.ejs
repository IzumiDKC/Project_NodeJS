<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <title>Danh sách sản phẩm</title>
  <style>
    body {
      font-family: 'Segoe UI', sans-serif;
      background-color: #f4f4f4;
      margin: 0;
      padding: 20px;
    }

    h1 {
      text-align: center;
      color: #333;
    }

    .product-grid {
      display: grid;
      grid-template-columns: repeat(auto-fill, minmax(250px, 1fr));
      gap: 20px;
      padding: 20px;
    }

    .product-card {
      background-color: #fff;
      border-radius: 8px;
      box-shadow: 0 2px 8px rgba(0,0,0,0.1);
      overflow: hidden;
      transition: transform 0.2s ease;
    }

    .product-card:hover {
      transform: translateY(-5px);
    }

    .product-image {
      width: 100%;
      height: 200px;
      object-fit: cover;
      background-color: #eee;
    }

    .product-content {
      padding: 15px;
    }

    .product-title {
      font-size: 18px;
      font-weight: bold;
      margin: 0 0 10px 0;
      color: #333;
    }

    .product-description {
      font-size: 14px;
      color: #666;
      height: 50px;
      overflow: hidden;
    }

    .product-price {
      margin-top: 10px;
      color: #007bff;
      font-size: 16px;
      font-weight: bold;
    }

    .product-quantity,
    .product-category,
    .product-brand {
      font-size: 13px;
      color: #999;
    }

    .product-actions {
      display: flex;
      flex-wrap: wrap;
      gap: 10px;
      margin-top: 15px;
    }

    .product-actions button,
    .product-actions form button {
      background-color: #007bff;
      color: white;
      border: none;
      padding: 8px 12px;
      border-radius: 5px;
      cursor: pointer;
      font-size: 13px;
      transition: background-color 0.3s ease;
    }

    .product-actions button:hover,
    .product-actions form button:hover {
      background-color: #0056b3;
    }

    .action-links {
      margin: 20px;
      text-align: center;
    }

    .action-links a {
      margin: 0 10px;
      color: #007bff;
      text-decoration: none;
      font-weight: bold;
    }

    .action-links a:hover {
      text-decoration: underline;
    }
  </style>
</head>
<body>

  <%- include('../partials/header') %>

  <h1>Danh sách sản phẩm</h1>

  <div class="product-grid">
    <% products.forEach(product => { %>
      <div class="product-card">
        <img src="<%= product.urlImg || 'https://via.placeholder.com/300x200?text=No+Image' %>" alt="<%= product.name %>" class="product-image">
        <div class="product-content">
          <div class="product-title"><%= product.name %></div>
          <div class="product-description"><%= product.description %></div>
          <div class="product-price">Giá: <%= product.price %>₫</div>
          <div class="product-quantity">Số lượng: <%= product.quantity %></div>
          <div class="product-category">Danh mục: <%= product.category?.name || 'Không rõ' %></div>
          <div class="product-brand">Thương hiệu: <%= product.brand?.name || 'Không rõ' %></div>

          <div class="product-actions">
            <button onclick="window.location.href='/products/view/edit/<%= product.id %>'">Sửa</button>
            <button onclick="deleteProduct('<%= product._id %>')">Xóa</button>
            <button onclick="window.location.href='/products/view/detail/<%= product.id %>'">Chi tiết</button>
            <form action="/cart/add/<%= product._id %>" method="POST">
              <button type="submit">Thêm vào giỏ</button>
            </form>
          </div>
        </div>
      </div>
    <% }) %>
  </div>

  <div class="action-links">
    <a href="/products/view/create">➕ Thêm sản phẩm mới</a>
    <a href="/cart/view">🛒 Xem giỏ hàng</a>
  </div>

  <%- include('../partials/footer') %>
</body>
</html>

<script>
  function deleteProduct(productId) {
    if (confirm("Bạn có chắc chắn muốn xóa sản phẩm này?")) {
      fetch(`/products/${productId}`, {
        method: 'DELETE',
        headers: {
          'Content-Type': 'application/json'
        }
      })
      .then(response => {
        if (response.ok) {
          alert("Xoá thành công");
          window.location.href = '/products/view/all';
        } else {
          return response.json().then(data => {
            throw new Error(data.message || "Xoá thất bại");
          });
        }
      })
      .catch(error => {
        alert("Lỗi: " + error.message);
      });
    }
  }
</script>
