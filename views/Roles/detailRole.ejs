<!DOCTYPE html>
<html>
<head>
  <title>Chi tiết Role</title>
  <style>
    body {
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      background-color: #f2f4f8;
      margin: 0;
      padding: 0;
    }

    .container {
      max-width: 600px;
      margin: 50px auto;
      background: #ffffff;
      padding: 30px;
      border-radius: 12px;
      box-shadow: 0 4px 20px rgba(0, 0, 0, 0.05);
    }

    h1 {
      color: #333;
      margin-bottom: 20px;
      font-size: 24px;
      border-bottom: 2px solid #eee;
      padding-bottom: 10px;
    }

    p {
      font-size: 16px;
      margin: 12px 0;
      color: #555;
    }

    strong {
      color: #000;
    }

    .btn-back {
      display: inline-block;
      margin-top: 25px;
      padding: 10px 18px;
      background-color: #007bff;
      color: #fff;
      text-decoration: none;
      border-radius: 6px;
      transition: background-color 0.2s ease-in-out;
    }

    .btn-back:hover {
      background-color: #0056b3;
    }
  </style>
</head>
<body>
  <%- include('../partials/header') %>
  <div class="container">
    <h1>Chi tiết Role</h1>
    <p><strong>ID:</strong> <%= role._id %></p>
    <p><strong>Tên Role:</strong> <%= role.name %></p>
    <p><strong>Mô tả:</strong> <%= role.description || 'Không có mô tả' %></p>
    <a href="/roles/view/all" class="btn-back">← Quay lại danh sách</a>
  </div>
  <%- include('../partials/footer') %>
</body>
</html>
